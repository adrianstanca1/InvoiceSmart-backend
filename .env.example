# Database
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_NAME=invoicesmart
DB_PASSWORD=change_me_in_production
DATABASE_URL=postgresql://postgres:change_me_in_production@127.0.0.1:5432/invoicesmart?sslmode=disable

# Security — rotate in production
JWT_SECRET=change_this_to_a_random_64_char_string_in_production

# Server
PORT=3008
NODE_ENV=production

# AI
OLLAMA_URL=http://127.0.0.1:11434/api/generate

# CORS (comma-separated origins, empty = allow all in dev)
# CORS_ORIGINS=https://app.example.com
